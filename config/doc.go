// Package config loads configuration from defaults, the working-directory file,
// explicitly selected files, and command-line flags, and saves preferences to
// the selected file.
// It should not depend on game runtime state.
package config
